@model IEnumerable<WebApplication2.Models.Position>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Latitude)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Longtitude)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Latitude)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Longtitude)
        </td>
        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_position }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_position }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_position })
        </td>
    </tr>
}

</table>
